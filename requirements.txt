# Medical VQA Framework Requirements

# Core ML
torch>=2.0.0
transformers>=4.40.0
datasets>=2.19.0
accelerate>=0.27.0

# Training
trl>=0.8.0
peft>=0.10.0
bitsandbytes>=0.43.0

# Vision
Pillow>=10.0.0
timm>=0.9.0  # Required for InternVL

# Utilities
numpy>=1.24.0
tqdm>=4.65.0

# Optional: Flash Attention (recommended)
# pip install flash-attn --no-build-isolation
